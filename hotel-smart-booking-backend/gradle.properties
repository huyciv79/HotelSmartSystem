# Set JVM options to prevent Out Of Memory and crash on systems with many cores but limited paging files
org.gradle.jvmargs=-Xms64m -Xmx192m -XX:MaxMetaspaceSize=128m -XX:+UseSerialGC
org.gradle.daemon=false
